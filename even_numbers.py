#This uses the range function wrapped in the list function to print the numbers from 2 (first arg) to 10 (second arg) incrementing by 2 (3rd arg)

even_numbers = list(range(2, 11, 2))
print(even_numbers)

